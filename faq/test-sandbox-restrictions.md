# Test (TSX) Sandbox Restrictions

If you encounter issues trying to run `pnpm test` in the Claude sandbox, run it as an unsandboxed Bash command. You should have permissions to do so. Be sure to run other non-test commands in the sandbox though.

Be sure to run `pnpm test`, not `npm test`.
